PUBLIC_FIREBASE_API_KEY=your_firebase_api_key_here
PUBLIC_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
PUBLIC_FIREBASE_PROJECT_ID=your-project-id
PUBLIC_FIREBASE_APP_ID=1:1234567890:web:abcdef123456

# Notes:
# - Copy this to .env and replace values with your Firebase project's web configuration.
# - These variables are public (client-side) and safe to use for frontend-only Firebase usage.
# - After setting these, install firebase: npm install firebase